<nav class="navbar navbar-default">
    <div class="container-fluid">

        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse"
                    data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="{{ path('root') }}">MyHS</a>
        </div>

        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            {% if is_granted("IS_AUTHENTICATED_FULLY") or is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <ul class="nav navbar-nav">
                    <li>
                        <a href="{{ path('root') }}">{{ 'app.dashboard.label'|trans }}</a>
                    </li>
                    <li>
                        <a href="{{ path('fos_user_profile_show') }}">{{ 'layout.account'|trans({}, 'FOSUserBundle') }}</a>
                    </li>
                    <li>
                        <a href="{{ path('patient_index') }}">{{ 'app.patient.plural_label'|trans }}</a>
                    </li>
                    <li>
                        <a href="{{ path('product_index') }}">{{ 'app.product.plural_label'|trans }}</a>
                    </li>
                    <li>
                        <a href="{{ path('treatment_index') }}">{{ 'app.treatment.plural_label'|trans }}</a>
                    </li>
                    <li>
                        <a href="{{ path('invoice_index') }}">{{ 'app.invoice.plural_label'|trans }}</a>
                    </li>
                    {#
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button"
                           aria-haspopup="true" aria-expanded="false">{{ 'app.treatment.plural_label'|trans }}<span
                                    class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li>
                                <a href="#">{{ 'app.treatment.plural_label'|trans }}</a>
                            </li>
                            <li>
                                <a href="{{ path('treatment_note_template_index') }}">{{ 'app.treatment_note_template.plural_label'|trans }}</a>
                            </li>
                        </ul>
                    </li>
                    #}
                </ul>
            {% endif %}

            <ul class="nav navbar-nav navbar-right">
                {% if is_granted("IS_AUTHENTICATED_FULLY") or is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                    <li>
                        <a href="{{ path('fos_user_security_logout') }}">{{ 'layout.logout'|trans({}, 'FOSUserBundle') }}</a>
                    </li>
                {% else %}
                    <li>
                        <a href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
                    </li>
                    <li>
                        <a href="{{ path('fos_user_registration_register') }}">{{ 'layout.register'|trans({}, 'FOSUserBundle') }}</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
