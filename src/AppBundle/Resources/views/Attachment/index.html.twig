{% extends 'inner.html.twig' %}

{% block header %}
    {{ 'app.attachment.plural_label'|trans }}
{% endblock %}

{% block body %}

    {% embed "buttons.html.twig" %}
        {% block buttons %}
            <a href="{{ path('attachment_create') }}" class="btn btn-primary">{{ 'app.attachment.new'|trans }}</a>
        {% endblock %}
    {% endembed %}

    <div class="navigation">
        {{ knp_pagination_render(attachments) }}
    </div>

    {% include "@App/Attachment/include/list.html.twig" %}

{% endblock %}
