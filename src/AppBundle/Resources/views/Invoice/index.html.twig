{% extends 'inner.html.twig' %}

{% block header %}
    {{ 'app.invoice.plural_label'|trans }}
{% endblock %}

{% block body %}

    {% embed "buttons.html.twig" %}
        {% block buttons %}
            <a href="{{ path('invoice_create') }}" class="btn btn-primary">{{ 'app.invoice.new'|trans }}</a>
        {% endblock %}
    {% endembed %}

    <div class="navigation">
        {{ knp_pagination_render(invoices) }}
    </div>
    
    {% include "@App/Invoice/include/list.html.twig" %}

{% endblock %}
