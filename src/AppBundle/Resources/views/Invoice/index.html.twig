{% extends 'inner.html.twig' %}

{% block header %}
    {{ 'app.invoice.plural_label'|trans }}
{% endblock %}

{% block body %}

    {% embed "buttons.html.twig" %}
        {% block buttons %}
            <a href="{{ path('invoice_create') }}" class="btn btn-primary">{{ 'app.invoice.new'|trans }}</a>
        {% endblock %}
    {% endembed  %}

    <table class="table table-hover table-bordered">
        <thead>
        <tr>
            <th>{{ 'app.invoice.label'|trans }}</th>
            <th>{{ 'app.action.plural_label'|trans }}</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in invoices %}
            <tr>
                <td><a href="{{ path('invoice_view', { 'id': entity.id }) }}">{{ entity }}</a></td>
                <td>
                    <a href="{{ path('invoice_update', { 'id': entity.id }) }}">{{ 'app.action.edit'|trans }}</a> /
                    <a href="{{ path('invoice_delete', { 'id': entity.id }) }}">{{ 'app.action.delete'|trans }}</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}
