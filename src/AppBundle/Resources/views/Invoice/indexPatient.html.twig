{% extends '@App/Patient/layout.html.twig' %}

{% block body %}

    {% embed "buttons.html.twig" %}
        {% block buttons %}
            <a href="{{ path('patient_invoice_create',{'patient':app_hash(entity)}) }}"
               class="btn btn-primary">{{ 'app.invoice.new'|trans }}</a>
        {% endblock %}
    {% endembed %}

    <div class="app-grid">
        <div class="app-grid-pagination">
            {{ knp_pagination_render(entities) }}
        </div>

        {% include "@App/Invoice/include/listPatient.html.twig" %}
    </div>

{% endblock %}
