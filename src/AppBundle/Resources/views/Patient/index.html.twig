{% extends 'inner.html.twig' %}

{% block header %}
    {{ 'myhs.patient.plural_label'|trans }}
{% endblock %}

{% block body %}

    <table>
        <thead>
        <tr>
            <th>Name</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in patients %}
            <tr>
                <td><a href="{{ path('patient_view', { 'id': entity.id }) }}">{{ entity }}</a></td>
                <td>
                    <a href="{{ path('patient_update', { 'id': entity.id }) }}">Edit</a> /
                    <a href="{{ path('patient_delete', { 'id': entity.id }) }}">Delete</a>
                    </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('patient_create') }}">{{ 'myhs.patient.new'|trans }}</a>
        </li>
    </ul>

{% endblock %}
