{% extends 'inner.html.twig' %}

{% block header %}
    {{ 'myhs.patient.plural_label'|trans }}
{% endblock %}

{% block body %}

    {% embed "buttons.html.twig" %}
        {% block buttons %}
            <a href="{{ path('patient_create') }}" class="btn btn-primary">{{ 'myhs.patient.new'|trans }}</a>
        {% endblock %}
    {% endembed  %}

    <table class="table table-hover table-bordered">
        <thead>
        <tr>
            <th>{{ 'myhs.patient.label'|trans }}</th>
            <th>{{ 'myhs.email'|trans }}</th>
            <th>{{ 'myhs.phone.label'|trans }}</th>
            <th>{{ 'myhs.appointment.next'|trans }}</th>
            <th>{{ 'myhs.action.plural_label'|trans }}</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in patients %}
            <tr>
                <td><a href="{{ path('patient_view', { 'id': entity.id }) }}">{{ entity }}</a></td>
                <td>
                    {% if entity.email %}
                        <a href="mailto:{{ entity.email }}">{{ entity.email }}</a>
                    {% endif %}
                </td>
                <td>
                    {% if entity.phones|length > 0 %}
                        <a href="tel:{{ entity.phones[0] }}">{{ entity.phones[0] }}</a>
                    {% endif %}
                </td>
                <td></td>
                <td>
                    <a href="{{ path('patient_update', { 'id': entity.id }) }}">{{ 'myhs.action.edit'|trans }}</a> /
                    <a href="{{ path('patient_delete', { 'id': entity.id }) }}">{{ 'myhs.action.delete'|trans }}</a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    {{ parent() }}

{% endblock %}
