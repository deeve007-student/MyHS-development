{% extends 'inner.html.twig' %}
{% set header = entity.id ? entity : 'app.patient.new'|trans %}

{% block subnavigation %}
    {{ knp_menu_render('patient', {'currentClass': 'active', 'template':'AppBundle:Menu:innerMenu.html.twig'}) }}
{% endblock %}
