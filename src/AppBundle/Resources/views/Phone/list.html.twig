{% if entity.phones|length > 0 %}
    {% for phone in entity.phones %}
        {{ macros.detailRowHtml(phone.phoneType, (macros.renderPhone(phone, phone.phoneNumber))) }}
    {% endfor %}
{% endif %}
