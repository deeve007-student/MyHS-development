{% extends 'inner.html.twig' %}

{% block header %}
    {{ entity }}
{% endblock %}

{% block body %}

    {% embed "buttons.html.twig" %}
        {% block buttons %}
            <a href="{{ path('treatment_update', { 'id': entity.id }) }}" class="btn btn-primary">{{ 'app.treatment.edit'|trans }}</a>
            <a href="{{ path('treatment_delete', { 'id': entity.id }) }}" class="btn btn-danger">{{ 'app.treatment.delete'|trans }}</a>
        {% endblock %}
    {% endembed  %}

    {% embed "subheading.html.twig" %}
        {% block subheading %}
            {{ 'app.general'|trans }}
        {% endblock %}
    {% endembed %}

    <div><span class="fieldLabel">{{ 'app.treatment.label'|trans }}:</span> {{ entity.name }}</div>
    <div><span class="fieldLabel">{{ 'app.treatment.price'|trans }}:</span> {{ entity.price|price }}</div>

{% endblock %}
